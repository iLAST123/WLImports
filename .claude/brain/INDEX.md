# Brain — WLimports

Índice das notas vivas do projeto. Atualizar 1 linha por nota a cada ciclo.

- `status.md` — estado atual do projeto e pendências (2026-07-21: missão 7 concluída — redesign "superfície clara" estilo Aesop + PLP dedicada `/catalogo`, deploy publicado via auto-deploy GitHub e validado por smoke em produção; aguardando validação visual do dono; missões 1–6 abaixo).
- `decisoes/DEC-001-stack-e-arquitetura.md` — ADR: Next.js App Router + API Route para Bling, mock-first, vídeos locais, tokens Tailwind v4, motion respeitando reduced-motion.
- `decisoes/DEC-002-bling-oauth-refresh.md` — ADR: OAuth refresh-token rotativo (Basic header, endpoint `www`, persistência em `.bling-tokens.json`), proxy SSRF-safe de imagem, rota `force-dynamic`; limitação conhecida de TokenStore serverless.
- `decisoes/DEC-003-imagem-original-detalhe-lazy.md` — ADR: imagem original via `GET /produtos/{id}` lazy no proxy `/api/imagem`, cache 2 camadas sem Redis, fila serial 350ms com teto 8, fallback miniatura, hardening Content-Type/nosniff.
- `decisoes/DEC-004-ecommerce-pdp-carrinho-checkout.md` — ADR: catálogo → e-commerce (PDP `/produto/[id]`, carrinho em localStorage, checkout SIMULADO sem gateway); detalhe do Bling unificado (1 request serve imagem+descrição+galeria), descrição do ERP renderizada como texto puro (nunca `dangerouslySetInnerHTML`), proxy de imagem com `&i=<n>`, fator UAU não-bloqueante.
- `decisoes/DEC-005-superficie-clara-aesop.md` — ADR: duas superfícies (escura/clara) por token semântico remapeado via `[data-superficie]`, site híbrido editorial (home escura, loja clara), PLP `/catalogo` dedicada sem chamada nova ao Bling, degradação honesta de campos editoriais só-mock, `separarVolume()` conservador; bronze substitui dourado no claro por contraste AA.
- `referencias-aesop.md` — nota de pesquisa de campo (não-ADR): computed styles reais da aesop.com (cor, tipografia, grid, IA de header/PLP/PDP), base factual da DEC-005, com confronto explícito contra os campos que o catálogo real do dono efetivamente tem.
- `aprendizados.md` — gotchas técnicos: **`@next/swc-darwin-arm64` oco derruba o `next build` local (fix: reinstalar o pacote + limpar `.next`)**, `next dev`/`build` local dispara OAuth real do Bling (receita segura com env vazias), ESLint do Next 16 trata `setState` em `useEffect` como erro, scroll lock precisa de `lenis.stop()`, `backdrop-blur` prende filho `fixed`, delay do Framer Motion, screenshot fullPage vs whileInView, contrato da listagem Bling v3 (imagemURL = miniatura 70x70; original só no detalhe, spec OpenAPI vence o client tipado), restrição de nome de pasta do create-next-app, gotchas do OAuth refresh do Bling (www obrigatório, Basic header, rotação de refresh token, revalidate em route handler no build, NBSP no BRL formatado), ioredis lazyConnect, env var da Vercel só vale em deployment novo.
- `preferencias.md` — preferências do projeto: idioma, paleta/tipografia fechadas, commits em português, escrita single-threaded.
